script(src="/javascripts/map.js")

h1= title
p Welcome to #{title}


div#flat(ng-controller="MapCtrl",style="{{flatStyle}}")
  div(title="{{poi.Name}}",ng-repeat="poi in pois",class="marker open-{{poi.OpenToday}}", style="{{poi.style}}")
    img(src="{{poi.IconUrl}}")
    
